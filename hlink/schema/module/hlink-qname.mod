<!-- ...................................................................... -->
<!-- HLink First Working Draft QName (Qualified Name) Module .............. -->
<!-- file: hlink-qname.mod
     

     
     Copyright 2010 Wakaba <wakaba@suikawiki.org>, All Rights Reserved.
     
     Permission to use, copy, modify and distribute this DTD and its
     accompanying documentation for any purpose and without fee is hereby
     granted in perpetuity, provided that the above copyright notice and
     this paragraph appear in all copies.  The copyright holders make no
     representation about the suitability of the DTD for any purpose.
     
     It is provided "as is" without expressed or implied warranty.
     
     Revision: 2010-06-20T04:51:18+00:00 (Generated by mkdtds/1.7)

     R4.9.18  URLs updated.
     
     This DTD module is identified by the SYSTEM identifier:
     
       SYSTEM "https://suika.suikawiki.org/gate/cvs/*checkout*/markup/hlink/schema/module/hlink-qname.mod"
     
     ...................................................................... -->

<!-- HLink QName (Qualified Name) Module

     This module is contained in two parts, labeled Section 'A' and 'B':
     
       Section A declares parameter entities to support namespace-qualified
       names, namespace declarations, and name prefixing.
     
       Section B declares parameter entities used to provide namespace-qualified
       names for all element types and global attribute names.
-->

<!ENTITY % sgml.tag.minimizable "IGNORE">

<!ENTITY % NS.prefixed "IGNORE">

<!-- Section A: XML Namespace Framework :::::::::::::::::::::::::: -->

<!-- 1. Declare a %hlink.prefixed; conditional section keyword, used
        to activate namespace prefixing. -->
<!ENTITY % hlink.prefixed "IGNORE">

<!ENTITY % hlink.global.attrs.prefixed "IGNORE">

<!ENTITY % hlink.xsi.attrs "INCLUDE">

<!-- 2. Declare a parameter entity %hlink.xmlns; containing
        the URI reference used to identity the namespace. -->
<!ENTITY % hlink.xmlns "http://www.w3.org/2002/06/hlink">

<!-- 3. Declare parameter entity %hlink.prefix; containing
        the default namespace prefix string to use when prefixing
        is enabled. This may be overridden in the DTD driver or the
        internal subset of a document instance.
        
        NOTE: As specified in XML Namespace speficications, the namespace
        prefix serves as a proxy for the URI reference, and is not in itself
        significant. -->
<!ENTITY % hlink.prefix "hlink">

<!-- 4. Declare parameter entity %hlink.pfx; containing the
        colonized prefix (e.g, '%hlink.prefix;:') used when
        prefixing is active, an empty string when it is not. -->
<![%hlink.prefixed;[
<!ENTITY % hlink.pfx "%hlink.prefix;:">
]]>
<!ENTITY % hlink.pfx "">

<!-- declare qualified name extensions here ............ -->
<!ENTITY % hlink-qname-extra.mod "">
%hlink-qname-extra.mod;

<!-- 5. The parameter entity %hlink.xmlns.extra.attrib; may be
        redeclared to contain any foreign namespace declaration
        attributes for namespaces embedded.  The default
        is an empty string. -->
<!ENTITY % hlink.xmlns.extra.attrib "">

<!-- The parameter entity %URI.datatype; should already be defined in
     Datatype module. -->
<!ENTITY % URI.datatype "CDATA">

<![%hlink.prefixed;[
<!ENTITY % hlink.xmlns.decl.attrib
	"xmlns:%hlink.prefix;	%URI.datatype;	#FIXED '%hlink.xmlns;'">
]]>
<!ENTITY % hlink.xmlns.decl.attrib
	"xmlns	%URI.datatype;	#FIXED '%hlink.xmlns;'">

<!-- Declare a parameter entity %XSI.prefix as a prefix to use for
     XML Schema Instance attributes. -->
<!ENTITY % XSI.prefix "xsi">

<!ENTITY % XSI.pfx "%XSI.prefix;:">

<!ENTITY % XSI.xmlns "http://www.w3.org/2001/XMLSchema-instance">

<!-- Declare a parameter entity %XSI.xmlns.attrib as support for
     the schemaLocation attribute. -->
<!ENTITY % XSI.xmlns.attrib
	"xmlns:%XSI.prefix;	%URI.datatype;	#FIXED '%XSI.xmlns;'">

<![%hlink.prefixed;[
<!ENTITY % NS.decl.attrib
	"%hlink.xmlns.decl.attrib;
	%hlink.xmlns.extra.attrib;
	%XSI.xmlns.attrib;">
]]>
<!ENTITY % NS.decl.attrib
	"%hlink.xmlns.extra.attrib;
	%XSI.xmlns.attrib;">

<!-- Declare a parameter entity containing all XML namespace declaration
     attributes used, including a default xmlns declaration when prefixing
     is inactive. -->
<![%hlink.prefixed;[
<!ENTITY % hlink.xmlns.attrib
	"%NS.decl.attrib;">
]]>
<!ENTITY % hlink.xmlns.attrib
	"%hlink.xmlns.decl.attrib;
	%NS.decl.attrib;">

<!-- Section B: HLink Qualified Names ::::::::::::::::::::::::::::: -->

<!-- placeholder for qualified name redeclarations -->
<!ENTITY % hlink-qname.redecl "">
%hlink-qname.redecl;

<!-- 6. This section declare parameter entities used to provide
        namespace-qualified names for all element types and global
        attribute names. -->
<!ENTITY % hlink.hlink.qname    "%hlink.pfx;hlink">
<!ENTITY % hlink.hlinks.qname   "%hlink.pfx;hlinks">

<!ENTITY % hlink.definition.attrib.qname "%hlink.prefix;:definition">


<!-- end of hlink-qname.mod -->
